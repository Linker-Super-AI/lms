# 快速开始：批量添加用户

## 🚀 5分钟快速上手

### 情况1：用户数量少（5个以内）

**直接编辑模板文件：**

```bash
# 1. 编辑 CSV 文件
nano /home/services/lms/data/users_template.csv

# 添加用户信息，格式：
# email,first_name,last_name,password
# zhangsan@linker.net,张三,,Linker@2024
# lisi@linker.net,李四,,Linker@2024

# 2. 保存后运行导入
cd /home/services/lms
docker compose -f docker-compose-prod.yml exec -T lms \
  bench --site 192.168.20.118 console < scripts/batch_create_users.py
```

✅ 完成！用户已创建并注册到课程。

---

### 情况2：用户数量多（几十个）

**使用 Excel 准备数据：**

```bash
# 1. 下载模板到本地电脑
scp root@192.168.20.118:/home/services/lms/data/users_template.csv ./

# 2. 用 Excel 打开，添加用户信息
#    列名：email, first_name, last_name, password
#    添加几十行用户数据

# 3. 保存为 CSV (UTF-8)，重命名为 users_batch.csv

# 4. 上传到服务器
scp users_batch.csv root@192.168.20.118:/home/services/lms/data/

# 5. 运行导入
ssh root@192.168.20.118
cd /home/services/lms
docker compose -f docker-compose-prod.yml exec -T lms \
  bench --site 192.168.20.118 console < scripts/batch_create_users.py
```

✅ 完成！

---

### 情况3：批量生成测试用户

**自动生成100个用户：**

```bash
# 1. 编辑生成脚本
nano /home/services/lms/scripts/generate_users_csv.py

# 修改这几行：
use_manual = False  # 使用自动生成
count = 100  # 生成数量
prefix = "student"  # 前缀：student001, student002...
domain = "linker.net"  # 域名
password = "Linker@2024"  # 统一密码

# 2. 运行生成脚本
python3 /home/services/lms/scripts/generate_users_csv.py

# 3. 运行导入脚本
cd /home/services/lms
docker compose -f docker-compose-prod.yml exec -T lms \
  bench --site 192.168.20.118 console < scripts/batch_create_users.py
```

✅ 100个用户自动创建完成！

---

## 📋 完整操作流程

```
┌─────────────────────────────────────────┐
│  准备用户数据                            │
│  - Excel 编辑                            │
│  - 或自动生成                            │
│  - 或直接编辑 CSV                        │
└─────────────┬───────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────┐
│  保存为 CSV 文件                         │
│  users_batch.csv 或 users_template.csv  │
└─────────────┬───────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────┐
│  运行导入脚本                            │
│  batch_create_users.py                  │
└─────────────┬───────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────┐
│  自动完成：                              │
│  ✓ 创建用户账号                         │
│  ✓ 设置密码                             │
│  ✓ 注册到课程                           │
└─────────────┬───────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────┐
│  导出登录信息给用户                      │
│  (邮箱 + 密码 + 登录地址)                │
└─────────────────────────────────────────┘
```

---

## 📄 CSV 文件格式

### 最简格式（只需2列）

```csv
email,first_name
zhangsan@linker.net,张三
lisi@linker.net,李四
```

系统会：
- 自动使用默认密码 `Linker@2024`
- 自动设置语言为中文
- 自动设置为门户用户

### 完整格式（4列）

```csv
email,first_name,last_name,password
zhangsan@linker.net,张三,销售部,Sales@2024
lisi@linker.net,李四,技术部,Tech@2024
wangwu@linker.net,王五,,Linker@2024
```

---

## 🔧 常用配置

### 修改目标课程

编辑 `/home/services/lms/scripts/batch_create_users.py`：

```python
COURSE_ID = "uf67dh57kl"  # 改为其他课程 ID
# 或留空不自动注册：
COURSE_ID = ""
```

### 修改 CSV 文件路径

```python
CSV_FILE = "/home/frappe/frappe-bench/sites/data/users_batch.csv"
# 或使用模板：
CSV_FILE = "/home/frappe/frappe-bench/sites/data/users_template.csv"
```

### 修改默认密码

```python
DEFAULT_PASSWORD = "YourCompany@2024"
```

---

## ✅ 成功检查清单

导入完成后检查：

- [ ] 访问用户列表：http://192.168.20.118:8001/app/user
- [ ] 确认用户数量正确
- [ ] 随机抽查几个用户能否登录
- [ ] 访问课程页面：http://192.168.20.118:8001/app/lms-course/uf67dh57kl
- [ ] 确认 Students 列表中有所有用户

---

## 📊 导出用户登录信息

导入完成后，创建一个表格发给所有用户：

| 序号 | 姓名 | 登录邮箱 | 初始密码 | 登录地址 | 课程地址 |
|------|------|----------|----------|----------|----------|
| 1 | 张三 | zhangsan@linker.net | Linker@2024 | [登录](http://192.168.20.118:8001/login) | [课程](http://192.168.20.118:8001/courses/uf67dh57kl) |
| 2 | 李四 | lisi@linker.net | Linker@2024 | [登录](http://192.168.20.118:8001/login) | [课程](http://192.168.20.118:8001/courses/uf67dh57kl) |

---

## ⚠️ 重要提示

1. **密码要求**
   - 至少8位
   - 大小写字母 + 数字
   - 建议格式：`Company@2024`

2. **邮箱唯一性**
   - 每个邮箱只能创建一次
   - 重复导入会跳过已存在的用户

3. **编码问题**
   - CSV 必须是 UTF-8 编码
   - Excel 保存时选择「CSV UTF-8」

4. **数量限制**
   - 建议单次导入不超过100个用户
   - 大量用户请分批导入

---

## 🆘 遇到问题？

### 问题1：CSV 文件读取失败

**检查：**
- 文件路径是否正确
- 文件编码是否为 UTF-8
- 文件格式是否正确（逗号分隔）

### 问题2：密码设置失败

**原因：** 密码不符合要求

**解决：** 使用符合要求的密码，如 `Linker@2024`

### 问题3：用户创建成功但无法登录

**检查：**
1. 用户类型是否为 `Website User`
2. 用户是否已启用
3. 密码是否正确设置

### 问题4：用户没有出现在课程中

**解决：**
- 检查 `COURSE_ID` 配置
- 手动在课程设置中添加学员

---

## 📞 快速命令参考

```bash
# 查看模板
cat /home/services/lms/data/users_template.csv

# 编辑模板
nano /home/services/lms/data/users_template.csv

# 生成测试用户 CSV
python3 /home/services/lms/scripts/generate_users_csv.py

# 批量导入用户
cd /home/services/lms
docker compose -f docker-compose-prod.yml exec -T lms \
  bench --site 192.168.20.118 console < scripts/batch_create_users.py

# 查看已创建的用户（后台）
# http://192.168.20.118:8001/app/user

# 查看课程学员（后台）
# http://192.168.20.118:8001/app/lms-course/uf67dh57kl
```
